# AWS 초보자 가이드: 필수 리소스 생성하기

> **목표**: AWS 콘솔에서 VPC, 서브넷, 보안 그룹, 키 페어, EC2 인스턴스를 직접 생성하고 필요한 ID 값들을 확인하는 방법을 배웁니다.

---

## 사전 준비

### 1. AWS 계정 생성
1. [AWS 공식 사이트](https://aws.amazon.com/ko/) 접속
2. **"무료 계정 만들기"** 클릭
3. 이메일, 비밀번호, 계정 이름 입력
4. 결제 정보 입력 (프리티어는 무료지만 카드 등록 필요)
5. 전화번호 인증 완료

### 2. 내 IP 주소 확인
보안 그룹 설정 시 필요합니다.

**확인 방법:**
1. 브라우저에서 [https://checkip.amazonaws.com](https://checkip.amazonaws.com) 접속
2. 표시되는 IP 주소 메모 (예: `123.456.78.90`)

### 3. 리전 설정
1. AWS 콘솔 우측 상단에서 리전 선택
2. **"아시아 태평양 (서울) ap-northeast-2"** 선택

---

## Step 1: VPC 생성 (가상 네트워크)

> **VPC란?** AWS에서 사용할 가상 네트워크 공간입니다.

### 콘솔에서 생성하기

1. AWS 콘솔 상단 검색창에 **"VPC"** 입력 → VPC 서비스 클릭
2. 좌측 메뉴에서 **"VPC"** 클릭
3. **"VPC 생성"** 버튼 클릭

### 설정값 입력

| 항목 | 값 | 설명 |
|------|-----|------|
| 이름 태그 | `codefill-vpc` | 식별용 이름 |
| IPv4 CIDR 블록 | `10.0.0.0/16` | IP 주소 범위 |
| IPv6 CIDR 블록 | 없음 | 선택 안함 |
| 테넌시 | 기본값 | 기본 선택 |

4. **"VPC 생성"** 클릭

### VPC ID 확인하기
- 생성 완료 후 목록에서 확인
- 형식: `vpc-0abc123def456789`
- 이 값을 **<VPC_ID>** 로 사용

---

## Step 2: 서브넷 생성

> **서브넷이란?** VPC 내부를 더 작은 네트워크로 나눈 것입니다.

### 퍼블릭 서브넷 생성

1. VPC 대시보드 좌측 메뉴 → **"서브넷"** 클릭
2. **"서브넷 생성"** 클릭

### 설정값 입력

| 항목 | 값 |
|------|-----|
| VPC ID | 방금 생성한 VPC 선택 |
| 서브넷 이름 | `codefill-public-subnet-1` |
| 가용 영역 | `ap-northeast-2a` |
| IPv4 CIDR 블록 | `10.0.1.0/24` |

3. **"서브넷 생성"** 클릭

### (선택) 두 번째 서브넷 생성
고가용성을 위해 다른 가용 영역에 추가 서브넷 생성

| 항목 | 값 |
|------|-----|
| 서브넷 이름 | `codefill-public-subnet-2` |
| 가용 영역 | `ap-northeast-2c` |
| IPv4 CIDR 블록 | `10.0.2.0/24` |

### 서브넷 ID 확인하기
- 형식: `subnet-0abc123def456789`
- 이 값을 **<SUBNET_ID>** 로 사용

---

## Step 3: 인터넷 게이트웨이 생성 및 연결

> **인터넷 게이트웨이란?** VPC가 인터넷과 통신할 수 있게 해주는 관문입니다.

### 생성하기

1. VPC 대시보드 좌측 메뉴 → **"인터넷 게이트웨이"** 클릭
2. **"인터넷 게이트웨이 생성"** 클릭
3. 이름: `codefill-igw`
4. **"인터넷 게이트웨이 생성"** 클릭

### VPC에 연결하기

1. 생성된 인터넷 게이트웨이 선택
2. **"작업"** → **"VPC에 연결"** 클릭
3. 앞서 생성한 VPC 선택
4. **"인터넷 게이트웨이 연결"** 클릭

---

## Step 4: 라우팅 테이블 설정

> **라우팅 테이블이란?** 네트워크 트래픽의 방향을 결정하는 규칙입니다.

### 라우팅 테이블 편집

1. VPC 대시보드 좌측 메뉴 → **"라우팅 테이블"** 클릭
2. VPC에 연결된 라우팅 테이블 선택
3. **"라우트"** 탭 → **"라우트 편집"** 클릭
4. **"라우트 추가"** 클릭

### 인터넷 라우트 추가

| 대상 | 타겟 |
|------|------|
| `0.0.0.0/0` | 인터넷 게이트웨이 (igw-xxx) 선택 |

5. **"변경 사항 저장"** 클릭

### 서브넷 연결

1. **"서브넷 연결"** 탭 클릭
2. **"서브넷 연결 편집"** 클릭
3. 퍼블릭 서브넷들 체크
4. **"연결 저장"** 클릭

---

## Step 5: 보안 그룹 생성

> **보안 그룹이란?** EC2 인스턴스의 방화벽 규칙입니다.

### 콘솔에서 생성하기

1. AWS 콘솔 검색창에 **"EC2"** 입력 → EC2 서비스 클릭
2. 좌측 메뉴 **"네트워크 및 보안"** → **"보안 그룹"** 클릭
3. **"보안 그룹 생성"** 클릭

### 기본 정보 입력

| 항목 | 값 |
|------|-----|
| 보안 그룹 이름 | `codefill-sg` |
| 설명 | `CodeFill application security group` |
| VPC | 앞서 생성한 VPC 선택 |

### 인바운드 규칙 추가

**"인바운드 규칙 추가"** 버튼을 클릭하여 다음 규칙들 추가:

| 유형 | 포트 범위 | 소스 | 설명 |
|------|----------|------|------|
| SSH | 22 | 내 IP (자동 입력) | SSH 접속용 |
| HTTP | 80 | 0.0.0.0/0 | 웹 서버 (선택) |
| HTTPS | 443 | 0.0.0.0/0 | HTTPS (선택) |
| 사용자 지정 TCP | 3000 | 내 IP | 프론트엔드 개발 서버 |
| 사용자 지정 TCP | 8000 | 내 IP | 백엔드 API 서버 |
| PostgreSQL | 5432 | 내 IP | 데이터베이스 (선택) |

> **주의**: "내 IP"를 선택하면 현재 IP가 자동 입력됩니다.
> IP가 바뀌면 규칙을 업데이트해야 합니다.

### 아웃바운드 규칙

기본값 유지 (모든 트래픽 허용)

4. **"보안 그룹 생성"** 클릭

### 보안 그룹 ID 확인하기
- 형식: `sg-0abc123def456789`
- 이 값을 **<SECURITY_GROUP_ID>** 로 사용

---

## Step 6: 키 페어 생성

> **키 페어란?** EC2 인스턴스에 SSH로 접속할 때 사용하는 암호화 키입니다.

### 콘솔에서 생성하기

1. EC2 대시보드 좌측 메뉴 → **"네트워크 및 보안"** → **"키 페어"** 클릭
2. **"키 페어 생성"** 클릭

### 설정값 입력

| 항목 | 값 | 설명 |
|------|-----|------|
| 이름 | `codefill-keypair` | 식별용 이름 |
| 키 페어 유형 | RSA | 기본값 |
| 프라이빗 키 파일 형식 | `.pem` (Mac/Linux) 또는 `.ppk` (Windows PuTTY) | 운영체제에 맞게 선택 |

3. **"키 페어 생성"** 클릭
4. **자동으로 .pem 파일 다운로드됨**

### 키 파일 보관 (중요!)

```bash
# Mac/Linux: 키 파일 권한 설정 (필수)
chmod 400 ~/Downloads/codefill-keypair.pem

# 안전한 위치로 이동
mv ~/Downloads/codefill-keypair.pem ~/.ssh/
```

> **경고**:
> - 키 페어 파일은 **절대 공유하지 마세요**
> - 분실 시 **재발급 불가** (새로 생성해야 함)
> - Git에 커밋하지 마세요 (.gitignore에 추가)

---

## Step 7: AMI 선택 (운영체제 이미지)

> **AMI란?** EC2 인스턴스의 운영체제 템플릿입니다.

### 추천 AMI 목록

| 용도 | AMI 이름 | 검색어 |
|------|---------|--------|
| 일반 (추천) | Amazon Linux 2023 | `al2023-ami` |
| Ubuntu | Ubuntu 22.04 LTS | `ubuntu/images/hvm-ssd/ubuntu-jammy-22.04` |
| 프리티어 | Amazon Linux 2023 (Free tier eligible) | `ami-` 로 시작하는 ID |

### AMI ID 찾기

1. EC2 대시보드 → **"인스턴스 시작"** 클릭
2. **"애플리케이션 및 OS 이미지"** 섹션 확인
3. 원하는 AMI 선택 시 **AMI ID** 표시됨
   - 형식: `ami-0abc123def456789`
   - 이 값을 **<AMI_ID>** 로 사용

### 서울 리전 주요 AMI ID (2024년 기준)

| OS | AMI ID | 비고 |
|----|--------|------|
| Amazon Linux 2023 | `ami-0c9c942bd7bf113a2` | 프리티어 |
| Ubuntu 22.04 LTS | `ami-0c9c942bd7bf113a2` | 프리티어 |

> **참고**: AMI ID는 주기적으로 업데이트됩니다. 콘솔에서 최신 ID를 확인하세요.

---

## Step 8: EC2 인스턴스 생성

### 콘솔에서 생성하기

1. EC2 대시보드 → **"인스턴스 시작"** 클릭

### 설정 단계별 입력

#### 1단계: 이름 및 태그
- 이름: `codefill-server`

#### 2단계: 애플리케이션 및 OS 이미지
- **Amazon Linux 2023** 또는 **Ubuntu 22.04** 선택
- **프리티어 사용 가능** 확인

#### 3단계: 인스턴스 유형
| 유형 | vCPU | 메모리 | 프리티어 |
|------|------|--------|----------|
| `t2.micro` | 1 | 1 GB | O (추천) |
| `t3.micro` | 2 | 1 GB | O |
| `t3.small` | 2 | 2 GB | X |

#### 4단계: 키 페어
- 앞서 생성한 `codefill-keypair` 선택

#### 5단계: 네트워크 설정
**"편집"** 클릭 후:

| 항목 | 값 |
|------|-----|
| VPC | 앞서 생성한 VPC 선택 |
| 서브넷 | 퍼블릭 서브넷 선택 |
| 퍼블릭 IP 자동 할당 | **활성화** |
| 보안 그룹 | **기존 보안 그룹 선택** → `codefill-sg` |

#### 6단계: 스토리지 구성
| 항목 | 값 | 설명 |
|------|-----|------|
| 크기 | 30 GiB | 프리티어 최대 30GB |
| 볼륨 유형 | gp3 | 범용 SSD |

#### 7단계: 고급 세부 정보 (선택)
- 기본값 유지

2. **"인스턴스 시작"** 클릭

### 인스턴스 ID 확인하기
- 생성 완료 후 목록에서 확인
- 형식: `i-0abc123def456789`
- 이 값을 **<INSTANCE_ID>** 로 사용

---

## Step 9: 탄력적 IP 할당 (선택)

> **탄력적 IP란?** 인스턴스를 재시작해도 변하지 않는 고정 IP입니다.

### 생성 및 연결

1. EC2 대시보드 → **"네트워크 및 보안"** → **"탄력적 IP"** 클릭
2. **"탄력적 IP 주소 할당"** 클릭
3. **"할당"** 클릭
4. 생성된 IP 선택 → **"작업"** → **"탄력적 IP 주소 연결"**
5. 인스턴스 선택 → **"연결"** 클릭

> **주의**: 탄력적 IP는 인스턴스에 연결되지 않은 상태로 두면 과금됩니다.

---

## Step 10: EC2 접속하기

### SSH 접속 (Mac/Linux)

```bash
ssh -i ~/.ssh/codefill-keypair.pem ec2-user@<퍼블릭_IP>
```

> **참고**: Ubuntu AMI의 경우 `ec2-user` 대신 `ubuntu` 사용

### SSH 접속 (Windows)

1. **PuTTY** 다운로드 및 설치
2. **PuTTYgen**으로 .pem → .ppk 변환
3. PuTTY에서 Host Name에 퍼블릭 IP 입력
4. Connection → SSH → Auth → Credentials에서 .ppk 파일 선택
5. **Open** 클릭

### 접속 확인

```bash
# 접속 성공 시
[ec2-user@ip-10-0-1-xxx ~]$

# 시스템 정보 확인
uname -a
```

---

## 생성된 리소스 ID 정리표

생성한 모든 리소스 ID를 여기에 기록하세요:

| 리소스 | ID | 메모 |
|--------|-----|------|
| VPC | `vpc-________________` | |
| 퍼블릭 서브넷 1 | `subnet-________________` | ap-northeast-2a |
| 퍼블릭 서브넷 2 | `subnet-________________` | ap-northeast-2c |
| 인터넷 게이트웨이 | `igw-________________` | |
| 보안 그룹 | `sg-________________` | |
| 키 페어 | `codefill-keypair` | 파일 위치: |
| AMI | `ami-________________` | |
| EC2 인스턴스 | `i-________________` | |
| 탄력적 IP | `________________` | (선택) |
| 내 IP 주소 | `________________` | 보안 그룹용 |

---

## 비용 관리 팁

### 프리티어 한도 (12개월)

| 서비스 | 무료 한도 |
|--------|----------|
| EC2 | t2.micro 750시간/월 |
| EBS | 30GB |
| 데이터 전송 | 15GB/월 |

### 비용 절약 방법

1. **사용하지 않는 인스턴스는 중지**
   - EC2 → 인스턴스 선택 → 인스턴스 상태 → 중지

2. **탄력적 IP 주의**
   - 연결되지 않은 탄력적 IP는 시간당 과금

3. **Billing 알림 설정**
   - 콘솔 → Billing → 예산 → 예산 생성

---

## 문제 해결

### SSH 접속 안 됨

| 증상 | 해결 방법 |
|------|----------|
| Connection timed out | 보안 그룹에 22번 포트 열려있는지 확인 |
| Permission denied | 키 파일 권한 확인 (`chmod 400`) |
| Host key verification failed | `~/.ssh/known_hosts`에서 해당 IP 삭제 |

### 인터넷 연결 안 됨

1. 인터넷 게이트웨이가 VPC에 연결되어 있는지 확인
2. 라우팅 테이블에 `0.0.0.0/0 → igw` 규칙 있는지 확인
3. 서브넷이 라우팅 테이블에 연결되어 있는지 확인

### 보안 그룹 수정

1. EC2 → 보안 그룹 → 해당 그룹 선택
2. 인바운드 규칙 편집
3. 규칙 추가/수정/삭제
4. 저장

---

## 다음 단계

- [ ] Docker 설치
- [ ] 애플리케이션 배포
- [ ] 도메인 연결 (Route 53)
- [ ] HTTPS 설정 (ACM + ALB)
- [ ] CI/CD 파이프라인 구축

---

> **작성일**: 2024년 1월
> **대상**: AWS 초보자
> **리전**: 서울 (ap-northeast-2)
